# 仓库内统一 LF；Windows 检出时不要把 shell 脚本改成 CRLF，
# 否则上传到 Linux 执行会报 $'\r': command not found。
* text=auto eol=lf

*.sh      text eol=lf
Caddyfile text eol=lf
*.service text eol=lf
*.conf    text eol=lf

# PowerShell 脚本保留 CRLF（Windows 原生使用）
*.ps1 text eol=crlf

*.png binary
